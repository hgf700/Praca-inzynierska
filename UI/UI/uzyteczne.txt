<Window x:Class="GeneticAlgorithm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GeneticAlgorithm" 
        xmlns:viewmodel="clr-namespace:GeneticAlgorithm.ViewModel"
        mc:Ignorable="d"
        Title="Work Schedule" Height="910" Width="auto"
        SizeChanged="Window_SizeChanged"
        Icon="/Icons/main.ico">
    <Window.Resources>
        <Style x:Key="CustomGridViewColumnHeaderStyle" TargetType="GridViewColumnHeader">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Work Schedule" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Center" 
                   FontSize="30"
                   Margin="10 20 10 10"/>

        <Button Grid.Column="1" 
                Grid.Row="0" 
                Content="Employees" 
                Background="#9ed5d9"
                Click="showEmployees"/>

        <Button Grid.Column="2" 
                Grid.Row="0" 
                Content="Employees preferences" 
                Background="#f5e949"
                Click="showEmployeesPreferences"/>

        <Button Grid.Column="3" 
                Grid.Row="0" 
                Content="Generate" 
                Click="OpenNewTab"/>

        <ListView x:Name="WorkScheduleListView"
                  Grid.ColumnSpan="4" 
                  ItemsSource="{Binding WorkSchedules}" 
                  Margin="15"
                  Grid.Row="1" 
                  Width="auto">
            <ListView.View>
                <GridView ColumnHeaderContainerStyle="{StaticResource CustomGridViewColumnHeaderStyle}">
                    <GridViewColumn Header="Date" 
                                    DisplayMemberBinding="{Binding FormattedDate}"/>
                    <GridViewColumn Header="Day" DisplayMemberBinding="{Binding DayOfWeek}"/>
                    <GridViewColumn Header="Change ID" DisplayMemberBinding="{Binding ChangeId}"/>
                    <GridViewColumn Header="Client Counter" DisplayMemberBinding="{Binding ClientCounter}"/>
                    <GridViewColumn Header="Employees">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ItemsControl ItemsSource="{Binding Employees}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="LightGray" Margin="2" Padding="4">
                                                <TextBlock Text="{Binding}" />
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Employees Counter" DisplayMemberBinding="{Binding EmployeeCounter}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>



<Window x:Class="GeneticAlgorithm.Prediction"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GeneticAlgorithm"
        mc:Ignorable="d"
        Title="Prediction"
        Height="900" Width="auto"
        SizeChanged="Window_SizeChanged"
        Icon="/Icons/prediction.ico">
    <Window.Resources>
        <Style x:Key="CustomGridViewColumnHeaderStyle" TargetType="GridViewColumnHeader">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
               Text="Work Schedule prediction for next week" 
               VerticalAlignment="Center" 
               HorizontalAlignment="Center" 
               FontSize="30"
               Margin="10 20 10 10"/>

        <Button Grid.Column="1" 
            Grid.Row="0" 
            Content="Accept" 
            Background="LightGreen"/>

        <Button Grid.Column="2" 
            Grid.Row="0" 
            Content="Decline" 
            Background="Red"/>

        <ListView x:Name="WorkScheduleListView"
              Grid.ColumnSpan="3" 
              ItemsSource="{Binding WorkSchedulesPredictions}" 
              Margin="15"
              Grid.Row="1" 
              Width="auto">
            <ListView.View>
                <GridView ColumnHeaderContainerStyle="{StaticResource CustomGridViewColumnHeaderStyle}">
                    <GridViewColumn Header="Date" DisplayMemberBinding="{Binding Date}"/>
                    <GridViewColumn Header="Day" DisplayMemberBinding="{Binding DayOfWeek}"/>
                    <GridViewColumn Header="Change ID" DisplayMemberBinding="{Binding ChangeId}"/>
                    <GridViewColumn Header="Employees">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ItemsControl ItemsSource="{Binding Employees}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="LightGray" Margin="2" Padding="4">
                                                <TextBlock Text="{Binding}" />
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Employees Counter" DisplayMemberBinding="{Binding EmployeeIdCount}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>



<Window x:Class="GeneticAlgorithm.Employees"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GeneticAlgorithm"
        mc:Ignorable="d"
        Title="Employees"
        Height="420" Width="900"
        SizeChanged="Window_SizeChanged"
        Icon="/Icons/employees.ico">
    <Window.Resources>
        <Style x:Key="CustomGridViewColumnHeaderStyle" TargetType="GridViewColumnHeader">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
            Text="Employees" 
            VerticalAlignment="Center" 
            HorizontalAlignment="Center" 
            FontSize="30"
            Margin="10 20 10 10"/>

        <ListView x:Name="EmployeeListView"
                  Grid.Row="1"
                  ItemsSource="{Binding Employees}" 
                  Margin="15"
                  Width="auto">
            <ListView.View>
                <GridView ColumnHeaderContainerStyle="{StaticResource CustomGridViewColumnHeaderStyle}">
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding EmployeeId}"/>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Last name" DisplayMemberBinding="{Binding LastName}"/>
                    <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}"/>
                    <GridViewColumn Header="Phone number" DisplayMemberBinding="{Binding PhoneNumber}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>





<Window x:Class="GeneticAlgorithm.EmployeesPreferences"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GeneticAlgorithm"
        mc:Ignorable="d"
        Title="EmployeesPreferences" 
        Height="450" 
        Width="900"
        SizeChanged="Window_SizeChanged"
        Icon="/Icons/preferences.ico">
    <Window.Resources>
        <Style x:Key="CustomGridViewColumnHeaderStyle" TargetType="GridViewColumnHeader">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.74*" />
            <ColumnDefinition Width="0.26*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Grid.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Padding" Value="5"/>
                    <Setter Property="HorizontalAlignment" Value="left"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </Grid.Resources>
            
            <TextBlock Grid.Row="0"
                       Text="Change 1 => 7:00 - 12:00"/>
            <TextBlock Grid.Row="1"
                       Text="Change 2 => 12:00 - 17:00"/>
            <TextBlock Grid.Row="2"
                       Text="Change 3 => 17:00 - 22:00"/>
        </Grid>

        <TextBlock Grid.Row="0" 
                   Grid.Column="0"
                   Text="Employees preferences for changes" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Center" 
                   FontSize="30"
                   Margin="10 20 10 10"/>

        <TextBlock Grid.Row="0"
                   Grid.Column="1"/>

        <ListView x:Name="EmployeePrederencesListView"
                  ItemsSource="{Binding EmployeePreferences}" 
                  Margin="15"
                  Grid.Row="1"
                  Grid.ColumnSpan="2"
                  Width="auto">
            <ListView.View>
                <GridView ColumnHeaderContainerStyle="{StaticResource CustomGridViewColumnHeaderStyle}">
                    <GridViewColumn Header="Employee" DisplayMemberBinding="{Binding Employee}"/>
                    <GridViewColumn Header="Monday" DisplayMemberBinding="{Binding Monday}"/>
                    <GridViewColumn Header="Tuesday" DisplayMemberBinding="{Binding Tuesday}"/>
                    <GridViewColumn Header="Wednesday" DisplayMemberBinding="{Binding Wednesday}"/>
                    <GridViewColumn Header="Thursday" DisplayMemberBinding="{Binding Thursday}"/>
                    <GridViewColumn Header="Friday" DisplayMemberBinding="{Binding Friday}"/>
                    <GridViewColumn Header="Saturday" DisplayMemberBinding="{Binding Saturday}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
